{
  "name": "web-novel-parser",
  "module": "mod.ts",
  "devDependencies": {
    "@rslib/core": "^0.1.5",
    "@types/bun": "latest",
    "@types/web": "^0.0.163",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "rslib build",
    "build:watch": "rslib build --watch",
    "test": "bun test",
    "start": "bun run build && bunx serve ."
  },
  "type": "module"
}